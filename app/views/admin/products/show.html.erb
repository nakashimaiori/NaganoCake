<div class="row">

  <div class="col-xs-10 col-xs-offset-2 top">
    <h1>商品詳細</h1>
  </div>

  <div class="col-xs-8 col-xs-offset-2">

      <div class="col-xs-5">
        <div class="field row">
          <%= attachment_image_tag @product, :image, :fill, 350, 300 %>
        </div>
      </div>

      <div class="col-xs-5">
        <table>
          <tbody>
            <tr>
              <td>商品名</td>
              <td class="font-size"><%= @product.name %></td>
            </tr>
            <tr>
              <td>商品説明</td>
              <td class="font-size"><%= @product.detail %></td>
            </tr>
            <tr>
              <td>ジャンル</td>
              <td class="font-size"><%= @product.genre.name %></td>
            </tr>
            <tr>
              <!-- 記述を整理する
              priceにbefore_price（税抜き価格）に消費税を乗算したもの（税込価格）を代入
              .roundで小数点以下を非表示
              .to_s(:delimited, delimiter: ',')で金額を三桁区切りで表示 -->
              <% price = @product.before_price * 1.1 %>
              <td>税込価格（税抜価格）</td>
              <td class="font-size"><%= price.round.to_s(:delimited, delimiter: ',') %> (<%= @product.before_price.to_s(:delimited, delimiter: ',') %>)円</td>
            </tr>
            <tr>
              <td>販売ステータス</td>
              <td>
                <% if @product.sale_status == true %>
                  販売中
                <% else %>
                  販売停止中
                <% end %>
              </td>
            </tr>
          </tbody>
        </table>
      </div>

      <div class="col-xs-offset-8">
        <%= button_to '編集する', edit_admin_product_path(@product), method: :get, class: "btn btn-lg btn-primary"%>
      </div>

  </div>
</div>