<div class="row">
  <div class="col-xs-8 col-xs-offset-2">

      <div class="col-xs-5">
        <h3>商品詳細</h3>
        <div class="field row">
          <%= attachment_image_tag @product, :image, :fill, 300, 300 %>
        </div>
      </div>

      <div class="col-xs-5">
        <table class="table">
          <tbody>
            <tr>
              <td>商品名</td><td><%= @product.name %></td>
            </tr>
            <tr>
              <td>商品説明</td><td><%= @product.detail %></td>
            </tr>
            <tr>
              <td>ジャンル</td><td><%= @product.genre.name %></td>
            </tr>
            <tr>
              <!-- 記述を整理する
              priceにbefore_price（税抜き価格）に消費税を乗算したもの（税込価格）を代入
              .roundで小数点以下を非表示
              .to_s(:delimited, delimiter: ',')で金額を三桁区切りで表示 -->
              <% price = @product.before_price * 1.1 %>
              <td>税込価格（税抜価格）</td><td><%= price.round.to_s(:delimited, delimiter: ',') %> (<%= @product.before_price.to_s(:delimited, delimiter: ',') %>)円</td>
            </tr>
            <tr>
              <td>販売ステータス</td>
              <td>
                <% if @product.sale_status == true %>
                  <p>販売中</p>
                <% else %>
                  <p>販売停止中</p>
                <% end %>
              </td>
            </tr>
          </tbody>
        </table>
        <%= button_to '編集する', edit_admin_product_path(@product), method: :get, class: "btn btn-primary" %>
      </div>
  </div>
</div>