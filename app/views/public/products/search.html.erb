<div class="container">
<div class="row">
<div class="col-xs-2"></div>


	<div class="col-xs-10">
		<div class="row">
			<h5><%= @genre.name %>一覧(全<%= @products.count %>件)</h5>
		</div>
			<!-- 商品表示をパーシャル化　-->
			<div class="row">
	<% @products.each do |product| %>
		<div class="col-lg-3">
			<%= link_to product_path(product.id) do %>
				<!-- <div class="over">
					<%#= attachment_image_tag item, :image, format: 'jpeg', width: "100%", height: "200px", style: "object-fit: cover;", fallback: "no_image.jpg" %>
				</div> -->
			<% end %>

			<ul style=>
				<li><%= product.name %></li>
				<!-- <li>¥<%#= (item.price * 1.1).to_i.to_s(:delimited) %></li> -->
			</ul>
		</div>
	<% end %>
</div>
	</div>

</div>		<!-- rowの閉じタグ -->
</div>